extends layout 

block content
    form(action="/new", method="post") 
        input(type="text" name="name" placeholder="List name")
        button.btn.btn-primary(type="submit") Create list
    button.btn.btn-secondary(type="button" onclick="window.history.back()") Back